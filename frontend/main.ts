// Placeholder entry point. Main window currently loads an external URL
// directly from Rust (src-tauri/src/main.rs); this file exists so the
// TypeScript and Biome toolchains have an input to operate on.
export {};
